"""HA-runtime tests for the custom_components/evisitor integration.

Skipped automatically when ``pytest-homeassistant-custom-component`` is
not installed (the heavy HA stack isn't a normal-test dep).
"""
